<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="br.com.profsalu.jogo.MainActivity"
    android:orientation="vertical"
    android:background="@color/fundo">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Digite um número entre 0 e 100:"
        android:textColor="@color/fonte"
        android:textSize="20sp"/>

    <!--Aceitando a confirmacao via evento de teclado (enviar)-->
    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:inputType="number"
        android:maxLength="3"
        android:id="@+id/numero"
        android:textColor="@color/fonte"
        android:imeOptions="actionDone"
        android:textSize="20sp"
        />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Tentativas: 0"
        android:layout_marginBottom="10dp"
        android:id="@+id/tentativas"
        android:textColor="@color/fonte"
        android:textSize="20sp"/>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Numeros digitados: "
        android:textColor="@color/fonte"
        android:textSize="20sp"/>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:minHeight="40dp"
        android:background="@color/historico"
        android:id="@+id/historico"
        android:gravity="center_vertical"
        android:paddingLeft="5dp"
        android:textColor="@color/fonte"
        android:textSize="25sp"/>

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="jogar"
        android:layout_gravity="center"
        android:id="@+id/jogar"
        android:textColor="@color/fonte"
        android:background="#FFD700"
        android:layout_marginTop="20dp"
        />

</LinearLayout>
